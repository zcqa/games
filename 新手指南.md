# 新手指南



加载时间可能有点长，耐心等待

## 基本操作

| 功能 | 键盘与鼠标 |
|---|---|
| 前进 / 后退 | `W` / `S` |
| 向左 / 向右移动 | `A` / `D` |
| 控制视角 | 移动鼠标 |
| 跳跃 | `Space` |
| 冲刺 | `Shift` |
| 蹲下 | `C` |
| 主交互 | `F` |
| 第二交互 | `E` |
| 打开或关闭背包 | `Tab` |
| 选择快捷栏 | 数字键 `1`–`8` |
| 切换快捷栏物品 | 鼠标滚轮 |
| 开始放置所选物品 | `V` |
| 旋转放置预览 | `R` |
| 确认放置 | 鼠标左键 |
| 取消放置 | 鼠标右键或 `Esc` |
| 暂停、返回或关闭当前界面 | `Esc` |

> 当前正式游戏中，`C` 用于蹲下；制作功能位于 `Tab` 打开的背包界面内。

## 如何查看任务

任务面板会显示任务名称、阶段、目标、进度和提示。目标前的符号含义如下：

| 符号 | 含义 |
|---|---|
| `●` | 当前可以完成的目标 |
| `○` | 尚未解锁，需要先完成前置目标 |
| `✓` | 已完成 |
| `×` | 已失败 |

部分目标会显示类似 `1 / 3` 的数量进度。当前目标下方如果出现 `Hint`，应优先按照提示寻找对应区域、资源或设备。

任务面板层级有些问题，需要先按esc,才能用鼠标向下滚动任务面板

## 如何与世界物体交互

1. 走近物体，将屏幕中央准星对准它。
2. 等待屏幕出现交互提示。
3. 按提示使用 `F` 或 `E`。

通常情况下：

- `F` 是主要操作，例如检查、采集、使用或启动设备。
- `E` 是第二操作，例如拾取、切换操作或执行物体提供的另一种交互。
- 实际按键应以屏幕上出现的提示为准。

如果按键没有反应，先确认准星确实对准了物体，并稍微靠近或调整观察角度。

## 拾取与采集资源

### 直接拾取

树枝、松散石块、零件、补给品等物体通常不需要工具。对准物体后，按屏幕提示使用 `F` 或 `E` 即可收入背包。

### 使用工具采集

树木、岩石等资源通常需要正确工具：

- 砍伐树木时，先选择斧头。
- 开采岩石时，先选择镐子。

使用数字键或鼠标滚轮选中工具，再对准资源按 `F`。部分资源需要连续交互数次，彻底采集完成后，材料才会进入背包。

如果出现需要某种工具的提示，说明当前快捷栏选中的物品不正确。切换工具后再次尝试。

## 背包与快捷栏

按 `Tab` 打开背包。背包界面同时包含物品栏、快捷栏和制作区域。

### 查看和整理物品

- 点击物品可以选中它。
- 选中物品后，再点击空白格可以移动物品。
- 可以直接将物品拖到快捷栏。
- 物品的右键菜单可用于拆分堆叠、旋转物品、整理背包或装备到快捷栏。

### 将物品放入快捷栏

可以使用以下任一方式：

- 把物品拖到快捷栏槽位。
- 选中物品后按数字键 `1`–`8`。
- 选中物品后点击对应快捷栏槽位。

关闭背包后，按数字键 `1`–`8` 或滚动鼠标滚轮即可选择快捷栏物品。



## 如何制作物品

1. 按 `Tab` 打开背包。
2. 查看制作区域中的配方。
3. 确认已经收集足够的材料。
4. 点击目标配方的制作按钮。
5. 制作成功后，成品会进入背包。



## 如何放置物品

帐篷、篝火、天线、净水装置等物品需要通过放置模式部署到场景中。

1. 确认物品已经在背包中。
2. 将物品装备到快捷栏。
3. 关闭背包，并选中该快捷栏槽位。
4. 按 `V` 进入放置模式。
5. 移动视角，将预览物体放到合适的地面上。
6. 按 `R` 旋转物体。每次旋转约 `15°`。
7. 预览为绿色时，按鼠标左键确认放置。
8. 如果不想放置，按鼠标右键或 `Esc` 取消。

预览颜色表示：

- 绿色：当前位置可以放置。
- 红色：当前位置无效，不能确认放置。

常见的无效原因包括：

- 准星没有指向可建造的地面。
- 地面坡度过大。
- 地面高低差过大，不够平整。
- 附近有树木、岩石、建筑或其他障碍物。
- 物品的完整占地范围没有得到地面支撑。



完成游戏后请填写问卷，并一起提交文件夹内最新生成的（每次运行都会对应生成一个）
```EXE所在目录\session_YYYYMMDD_HHMMSS.jsonl```
问卷链接
https://tally.so/r/0QzN5y